import 'package:flutter/material.dart';

class WorkoutListItemWidget extends StatelessWidget {
  const WorkoutListItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
